In this example the specification is taken from external YAML file
---
tags:
  -  cat_add
parameters:
  - name: 接口主体
    in: body
    schema:
      id: score（接口的名称）
      required:
        - id
        - name
        - age
      properties:
        id:
          type: number
          description: id
        name:
          type: string
          description: 姓名
        age:
          type: number
          description: 年龄
responses:
   200:
      description: 响应结果正确
   400:
      description: 响应结果错误